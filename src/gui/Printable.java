package gui;

/**
 * Interface for requiring for a 
 * class/enum providing a 'name' 
 * class, which must return the 
 * own class name.
 */
public interface Printable
{
    /**
     * Method required by interface
     * Printable.
     * @return Class name
     */
    public String name();
}
